<form method="post" action="{{ path('test_product_delete', {'id': test_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ test_product.id) }}">
    <button class="btn">Delete</button>
</form>
