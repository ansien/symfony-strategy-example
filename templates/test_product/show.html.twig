{% extends 'base.html.twig' %}

{% block title %}TestProduct{% endblock %}

{% block body %}
    <h1>TestProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ test_product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ test_product.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ test_product.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('test_product_index') }}">back to list</a>

    <a href="{{ path('test_product_edit', {'id': test_product.id}) }}">edit</a>

    {{ include('test_product/_delete_form.html.twig') }}
{% endblock %}
